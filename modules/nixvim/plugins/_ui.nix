{
  plugins = {
    neoscroll = {
      enable = true;
    };
    noice = {
      enable = true;
    };
    rainbow-delimiters = {
      enable = true;
      lazyLoad.settings.event = [ "BufReadPost" ];
    };
  };
}
