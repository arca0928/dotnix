{
  plugins = {
    neoscroll = {
      enable = true;
    };
    transparent = {
      enable = true;
      autoLoad = true;
    };
    noice = {
      enable = true;
    };
    rainbow-delimiters = {
      enable = true;
      lazyLoad.settings.event = [ "BufReadPost" ];
    };
  };
}
